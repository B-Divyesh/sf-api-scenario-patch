# Copy audit — repair round 1

The landing page was read aloud at 390 px. Its first screen states the job, audience,
and first action before the hero image.

## Landing sentences

| Words | Sentence |
| ---: | --- |
| 6 | Record API flows for Git review. |
| 12 | For small API teams reviewing multi-step requests without a shared API-client workspace. |
| 10 | Runs `asp demo` and shows its Markdown and YAML files. |
| 10 | It records no request or response body until you allow a route. |
| 10 | JSON field rules mask values before files are written. |
| 6 | The proxy accepts loopback addresses only. |
| 7 | The same sample makes the same patch. |
| 9 | Config and command line both must opt in. |
| 13 | A scenario patch holds requests, masked values, saved response values, and notes. |
| 10 | Use a bundled checkout retry. The demo uses no API traffic. |

All visitor-facing prose sentences are at most 22 words. No banned plain-words terms
are used.

## Terminology

| Concept | One term |
| --- | --- |
| Input | API flow |
| Output | scenario patch |
| Output files | Markdown and YAML files |
| Response values used later | saved response values |
| Browser or CLI sample | demo |
