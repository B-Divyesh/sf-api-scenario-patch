{
  "name": "api-scenario-patch-workspace",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --config site/vite.config.ts",
    "build:site": "vite build --config site/vite.config.ts",
    "build": "npm run build:site && cargo build --release --manifest-path cli/Cargo.toml && node scripts/copy-binary.mjs",
    "test": "cargo fmt --manifest-path cli/Cargo.toml -- --check && cargo clippy --workspace --all-targets --all-features -- -D warnings && cargo test --manifest-path cli/Cargo.toml && cargo build --bins --manifest-path cli/Cargo.toml && npm run build:site && node scripts/test-static-config.mjs && node scripts/test-cli-integration.mjs && playwright test"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.10.2",
    "@playwright/test": "1.58.2",
    "vite": "7.3.6"
  }
}
