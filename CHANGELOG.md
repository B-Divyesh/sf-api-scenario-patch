# Changelog

## 0.1.0 — 2026-08-28

- First release: opt-in reverse-proxy capture, local redaction, variable extraction,
  deterministic Markdown/YAML patches, gated replay, and static documentation site.
- Hardened capture privacy for query credentials, API-key headers, and environment-backed
  configured secrets; scalar variables now replace number and boolean JSON values.
- Made concurrent exchange limits strict, successful step numbering contiguous, and JSON
  error output machine-readable for both parser and validation failures.
- Added the Azure-native security/cache policy and mobile keyboard access for scrollable code.
