# Checkout retry sample

`asp demo` turns this realistic checkout retry into a Markdown and YAML scenario patch.
The sample includes a masked card number, a saved `order_id`, and a later retry that
uses that saved value. It is bundled with the CLI and never contacts an API.
